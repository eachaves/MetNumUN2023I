my_list.append sirve para  añadir  a lista un elemento
my_list.remove sirve para eliminar de la lista un elemento 
my_list[2] sirve para cambiar un elemento de la lista por otro
.replace reemplaza el caracter escogido por otro
my_float1 = float(my_string3) sirve para pasar de de string a float 
stock_set = set  para crear un conjunto sin datos duplicados

class stock en esta clase se definen unas operaciones y cada operacion tiene unos datos con los que trabaja
import numpy as np es para importar la libreria numpy
import pandas as pd importar la libreria pandas
pip install yfinance instalar el paquete yfinance
